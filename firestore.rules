rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /transactions/{doc} {
      allow read: if request.auth != null && request.auth.token.dashboard == true;
      allow write: if false;
    }
  }
}
